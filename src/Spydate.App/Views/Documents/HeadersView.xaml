<UserControl x:Class="Spydate.App.Views.Documents.HeadersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:docs="clr-namespace:Spydate.App.ViewModels.Documents"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=docs:HeadersDocumentViewModel}"
             Background="{DynamicResource Document.Background}">
    <UserControl.Resources>
        <DataTemplate x:Key="FieldRow" DataType="{x:Type docs:PropertyRow}">
            <Grid MinHeight="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="215" />
                    <ColumnDefinition Width="190" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Name}" Style="{StaticResource MonoText}" VerticalAlignment="Center"
                           Foreground="{DynamicResource Text.Secondary}" />
                <TextBlock Grid.Column="1" Text="{Binding Value}" Style="{StaticResource MonoText}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="2" Text="{Binding Note}" VerticalAlignment="Center" TextWrapping="Wrap"
                           Foreground="{DynamicResource Text.Tertiary}" />
            </Grid>
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="8,6">
        <StackPanel MaxWidth="1000" HorizontalAlignment="Left">
            <TextBlock Text="IMAGE_DOS_HEADER" Style="{StaticResource SectionHeader}" Margin="0,0,0,3" />
            <Border Style="{StaticResource Card}">
                <ItemsControl ItemsSource="{Binding Dos}" ItemTemplate="{StaticResource FieldRow}" />
            </Border>

            <TextBlock Text="IMAGE_FILE_HEADER" Style="{StaticResource SectionHeader}" />
            <Border Style="{StaticResource Card}">
                <ItemsControl ItemsSource="{Binding File}" ItemTemplate="{StaticResource FieldRow}" />
            </Border>

            <TextBlock Text="IMAGE_OPTIONAL_HEADER" Style="{StaticResource SectionHeader}" />
            <Border Style="{StaticResource Card}">
                <ItemsControl ItemsSource="{Binding Optional}" ItemTemplate="{StaticResource FieldRow}" />
            </Border>

            <TextBlock Text="Data directories" Style="{StaticResource SectionHeader}" />
            <Border Style="{StaticResource Card}" Padding="0">
                <DataGrid ItemsSource="{Binding Directories}" Style="{StaticResource DocGrid}" MaxHeight="420"
                          Background="Transparent">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Index}" Width="34" />
                        <DataGridTextColumn Header="Directory" Binding="{Binding Name}" Width="165" />
                        <DataGridTextColumn Header="RVA" Binding="{Binding Rva}" Width="105" />
                        <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="105" />
                        <DataGridTextColumn Header="Section" Binding="{Binding Section}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
            <Border Height="12" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
